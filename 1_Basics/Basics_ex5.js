// Explain why this code logs '510' instead of 15.
console.log('5' + 10);

/* The concatenation of a string ('5') and a number (10) coerces (coverts) the 
number into a string value. This is an example of implicit type coercion. */
